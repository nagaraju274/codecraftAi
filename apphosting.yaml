# Settings to manage and configure a Firebase App Hosting backend.
# https://firebase.google.com/docs/app-hosting/configure

# The runtime configuration for the backend.
runConfig:
  # The maximum number of server instances that can be running at the same time.
  # Increase this value if you'd like to automatically spin up more instances in response to increased traffic.
  maxInstances: 1
