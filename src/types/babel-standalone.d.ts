// This is a TypeScript declaration file for a JavaScript module that doesn't have its own types.

// Declare a module for "babel-standalone".
declare module "babel-standalone" {
    // Declare a constant 'Babel' of type 'any'.
    // This tells TypeScript that a 'Babel' object exists in this module, but we don't know its specific type.
    const Babel: any;
    // Export 'Babel' as the default export of this module.
    export default Babel;
  }
